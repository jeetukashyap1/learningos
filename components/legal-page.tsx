import Link from "next/link";

export function LegalPage({ title, summary, sections }: { title: string; summary: string; sections: { heading: string; body: string }[] }) { return <main className="container"><div style={{ maxWidth: 780, margin: "35px auto" }}><Link href="/" className="muted" style={{ fontSize: 12, fontWeight: 800 }}>← LearningOS home</Link><div className="eyebrow" style={{ marginTop: 36 }}>LEARNINGOS POLICY</div><h1 style={{ marginTop: 14 }}>{title}</h1><p className="muted" style={{ fontSize: 15, lineHeight: 1.8, marginTop: 18 }}>{summary}</p><div style={{ display: "grid", gap: 18, marginTop: 34 }}>{sections.map((section) => <section className="card pad" key={section.heading}><h2 style={{ fontSize: 19 }}>{section.heading}</h2><p className="muted" style={{ fontSize: 13, lineHeight: 1.8, marginTop: 10 }}>{section.body}</p></section>)}</div></div></main>; }

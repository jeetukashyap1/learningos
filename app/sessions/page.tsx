import Link from "next/link";
import { Monitor, Smartphone } from "lucide-react";
export default function SessionsPage(){return <main className="container"><Link href="/settings/security" className="muted" style={{fontSize:12,fontWeight:800}}>← Security</Link><div style={{maxWidth:700,margin:"30px auto"}}><div className="eyebrow">ACCOUNT SECURITY</div><h1 style={{marginTop:12}}>Active sessions</h1><div className="card pad" style={{marginTop:28}}>{[{icon:Monitor,label:"Windows desktop",detail:"Active now · This device"},{icon:Smartphone,label:"Mobile device",detail:"Last active yesterday"}].map(({icon:Icon,label,detail})=><div className="list-row" key={label}><Icon size={19}/><div style={{flex:1}}><strong style={{fontSize:13}}>{label}</strong><p className="muted" style={{fontSize:11,marginTop:4}}>{detail}</p></div><button className="btn btn-ghost">Sign out</button></div>)}</div></div></main>}

import Link from "next/link";
import { Bell, ChevronRight, LockKeyhole, Palette, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "@/components/app-shell";

const settings = [{ href: "/settings/account", icon: UserRound, title: "Account", detail: "Name, email, profile details" }, { href: "/settings/security", icon: LockKeyhole, title: "Security", detail: "Password, sessions, sign-in protection" }, { href: "/settings/privacy", icon: ShieldCheck, title: "Privacy", detail: "Data controls and visibility" }, { href: "/settings/preferences", icon: Palette, title: "Preferences", detail: "Theme, reminders, learning rhythm" }];
export default function SettingsPage() { return <main className="container"><PageHeader eyebrow="Make the space yours" title="Settings" description="Keep your learning environment comfortable, focused, and under your control." /><div className="card" style={{ maxWidth: 800 }}>{settings.map(({ href, icon: Icon, title, detail }) => <Link href={href} className="list-row" style={{ padding: "22px 24px" }} key={href}><Icon size={19} color="var(--orange)"/><div style={{ flex: 1 }}><strong>{title}</strong><p className="muted" style={{ fontSize: 12, marginTop: 4 }}>{detail}</p></div><ChevronRight size={16} className="muted"/></Link>)}</div><div className="card pad section" style={{ maxWidth: 800, display: "flex", gap: 14 }}><Bell size={18}/><div><h3>Notifications stay useful</h3><p className="muted" style={{ fontSize: 12, lineHeight: 1.6, marginTop: 6 }}>LearningOS only nudges you when a review or next action is genuinely helpful.</p></div></div></main>; }
